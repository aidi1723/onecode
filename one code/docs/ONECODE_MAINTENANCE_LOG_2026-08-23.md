# OneCode Maintenance Log - 2026-08-23

## Scope

Repository hygiene audit and cleanup of the `one code/` working directory.
No kernel/runtime code was changed in this pass.

## Audit Summary

- Full test suite (`.venv/bin/python -m unittest discover -s tests`): 907 tests
  passed, 1 environment-only skip.
- `scripts/verify-core.sh`: 234 core tests passed, `onecode doctor` returned
  `status: ok`.
- `scripts/check_source_quality.py src`: passed (`source quality ok`).
- Security spot-check: no `shell=True` in any `subprocess` call across
  `src/`; command/verifier/environment allowlists confirmed in
  `execution_tools.py`, `verifier.py`, `path_guard.py`; Web API auth uses
  `secrets.compare_digest` and defaults to loopback-only unauthenticated
  access (`src/onecode/web/auth.py`, `run_server` default `127.0.0.1`).
- `.env.local` (`OPENAI_API_KEY`) is correctly excluded by `.gitignore`
  (`.env.*` rule) and was never committed.

## Issue Found: Unrelated Files In Working Directory

`git status` on `one code/` showed untracked files that do not belong to the
OneCode project: `one code/output/` and `one code/tmp/` contained a generated
blog-design document (`OneCode_Agent_Kernel_Blog_Design_CN.docx`), its
generation script, and three rendering attempts (PDF + PNG pages) from an
unrelated content-writing task performed earlier in this working directory.

Neither `output/` nor `tmp/` was covered by `.gitignore`, so these files were
sitting untracked and undocumented inside the source tree.

(Separately, the git status snapshot at the start of this session also showed
several sibling top-level directories such as `onecode-librechat/`,
`oneword-agent-gateway/`, `agent交易所-open-source/`, `data/`, `home/`,
`reports/`, `schemas/` appearing under the `一 code/` path in `git status`
output. These belong to the parent `大字典` workspace, not to the OneCode
repository itself, and were out of scope for this cleanup — they were not
touched.)

## Action Taken

Moved (not deleted) the entire blog-design artifact set out of the OneCode
repository to a project-external location, preserving every render:

```text
~/Desktop/OneCode_Blog_Design_CN/
├── generate_onecode_blog_design_docx.py   (was tmp/docs/generate_onecode_blog_design_docx.py)
├── output-doc/
│   └── OneCode_Agent_Kernel_Blog_Design_CN.docx   (was output/doc/...)
├── rendered/       (was tmp/docs/rendered/    - PDF + 5 page PNGs)
├── rendered-v2/     (was tmp/docs/rendered-v2/  - PDF + 4 page PNGs)
└── rendered-v3/     (was tmp/docs/rendered-v3/  - PDF + 3 page PNGs)
```

After the move, `one code/output/` and `one code/tmp/` were removed (both are
now fully empty / non-existent — no residual empty directories left behind).

## Verification After Cleanup

- `ls output tmp` inside `one code/`: both report "No such file or directory".
- `git status --porcelain -- output tmp`: empty (nothing left to track or
  ignore).
- Destination directory contents verified with `find` — all 16 files present
  (1 script, 1 docx, 3 PDFs, 12 PNGs).

## Where To Look Next Time

- If someone asks "where did the blog design docx go", it is at
  `~/Desktop/OneCode_Blog_Design_CN/` (outside this repository, by design —
  it was never part of OneCode).
- `output/` and `tmp/` are **not** part of the normal OneCode build or test
  flow (confirmed: not referenced by `scripts/verify.sh`,
  `scripts/verify-core.sh`, or any test). If they reappear, they are most
  likely scratch space from an unrelated task run in this same working
  directory, not generated by OneCode itself.
- Consider adding `/output/` and `/tmp/` to `.gitignore` if this working
  directory continues to be reused for non-OneCode scratch work, to avoid
  re-triggering this same untracked-file finding.
