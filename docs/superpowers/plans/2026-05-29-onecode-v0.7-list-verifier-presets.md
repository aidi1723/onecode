# OneCode v0.7 List Verifier Presets Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a read-only CLI command that lists built-in verifier policy presets.

**Architecture:** Keep preset definitions in `onecode.kernel.verifier`. Add a pure serializer helper and expose it through a small CLI subcommand that only prints JSON.

**Tech Stack:** Python stdlib `argparse`, `json`, existing `unittest`.

---

## File Structure

- Modify `src/onecode/kernel/verifier.py`: add `verifier_policy_presets_summary()`.
- Modify `src/onecode/cli.py`: add `list-verifier-presets` parser and command handler.
- Modify `tests/test_verifier_policy_init_cli.py`: add CLI coverage for read-only preset listing.
- Modify `README.md`: add discovery command.
- Modify `tests/test_readme.py`: require README discovery command.

## Task 1: CLI Preset Listing

**Files:**
- Modify: `tests/test_verifier_policy_init_cli.py`
- Modify: `src/onecode/kernel/verifier.py`
- Modify: `src/onecode/cli.py`

- [ ] **Step 1: Add failing CLI test**

Add a test that calls:

```python
exit_code = main(["list-verifier-presets"])
```

Assert exit code `0`, JSON contains sorted preset ids `["python-compileall", "python-unittest"]`, and no `.onecode/verifier-policy.json` is written in a temp current directory.

- [ ] **Step 2: Run red test**

Run:

```bash
PYTHONPATH=src python3 -m unittest tests.test_verifier_policy_init_cli.VerifierPolicyInitCliTests.test_cli_list_verifier_presets_prints_read_only_summary -v
```

Expected: parser rejects unknown subcommand.

- [ ] **Step 3: Implement serializer and CLI handler**

Add `verifier_policy_presets_summary()` that returns `{"presets": [...]}` sorted by id. Add the `list-verifier-presets` subcommand and print the helper result.

- [ ] **Step 4: Run green test**

Run the same command.

Expected: pass.

## Task 2: README

**Files:**
- Modify: `README.md`
- Modify: `tests/test_readme.py`

- [ ] **Step 1: Add failing README assertion**

Require `python3 -m onecode list-verifier-presets` in `tests/test_readme.py`.

- [ ] **Step 2: Run red test**

Run:

```bash
PYTHONPATH=src python3 -m unittest tests.test_readme.ReadmeTests.test_readme_documents_core_cli_and_verification_paths -v
```

Expected: fail until README is updated.

- [ ] **Step 3: Document discovery command**

Add the command near the verifier policy workflow:

```bash
PYTHONPATH=src python3 -m onecode list-verifier-presets
```

- [ ] **Step 4: Run green test**

Run the same README test.

Expected: pass.

## Task 3: Verification

**Files:**
- Verify all changed files.

- [ ] **Step 1: Focused tests**

Run:

```bash
PYTHONPATH=src python3 -m unittest tests.test_verifier_policy_init_cli tests.test_readme tests.test_run_plan_cli -v
```

Expected: pass.

- [ ] **Step 2: Full verification**

Run:

```bash
bash scripts/verify.sh
```

Expected: compileall passes, unittest passes, doctor returns `status: ok`.

## Self-Review

- Spec coverage: read-only listing, sorted stable output, no file write, and README are covered.
- Placeholder scan: no deferred implementation notes remain.
- Type consistency: command and helper names match the spec.
