# OneCode v0.7 Local Demo Script Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a one-command local demo for the v0.7 verified `run-plan` workflow.

**Architecture:** Use a Bash script in `scripts/` that creates a temporary workspace, writes a JSON task plan, and invokes existing CLI commands. Keep the script deterministic and local-only.

**Tech Stack:** Bash, Python module entrypoint, existing unittest.

---

## File Structure

- Add `scripts/demo_v07.sh`: executable local demo script.
- Modify `tests/test_verify_script.py`: add demo script existence/content/runtime smoke tests.
- Modify `README.md`: document demo command.
- Modify `tests/test_readme.py`: require demo command in README.

## Task 1: Script Red Tests

**Files:**
- Modify: `tests/test_verify_script.py`
- Add: `scripts/demo_v07.sh`

- [ ] **Step 1: Add failing script tests**

Add tests that assert:

- `scripts/demo_v07.sh` exists and is executable.
- script text contains `list-verifier-presets`, `init-verifier-policy`, `run-plan`, `inspect`, and `list-runs`.
- running `PYTHON=<current interpreter> bash scripts/demo_v07.sh` succeeds and stdout includes `demo workspace`.

- [ ] **Step 2: Run red tests**

Run:

```bash
PYTHONPATH=src python3 -m unittest tests.test_verify_script.VerifyScriptTests.test_demo_v07_script_exists_and_is_executable tests.test_verify_script.VerifyScriptTests.test_demo_v07_script_documents_v07_commands tests.test_verify_script.VerifyScriptTests.test_demo_v07_script_runs_with_current_interpreter -v
```

Expected: fail because the script is missing.

- [ ] **Step 3: Add script**

Create `scripts/demo_v07.sh` with:

- `set -euo pipefail`
- interpreter selection matching `scripts/verify.sh`
- temp workspace unless `WORKSPACE` is set
- task plan JSON
- CLI calls for preset list, policy init, verified run-plan, inspect, and list-runs

- [ ] **Step 4: Make executable and run green tests**

Run:

```bash
chmod +x scripts/demo_v07.sh
PYTHONPATH=src python3 -m unittest tests.test_verify_script.VerifyScriptTests.test_demo_v07_script_exists_and_is_executable tests.test_verify_script.VerifyScriptTests.test_demo_v07_script_documents_v07_commands tests.test_verify_script.VerifyScriptTests.test_demo_v07_script_runs_with_current_interpreter -v
```

Expected: pass.

## Task 2: README

**Files:**
- Modify: `README.md`
- Modify: `tests/test_readme.py`

- [ ] **Step 1: Add failing README assertion**

Require `bash scripts/demo_v07.sh` in README test.

- [ ] **Step 2: Run red README test**

Run:

```bash
PYTHONPATH=src python3 -m unittest tests.test_readme.ReadmeTests.test_readme_documents_core_cli_and_verification_paths -v
```

Expected: fail until README is updated.

- [ ] **Step 3: Update README**

Add a short `Local Demo` section:

```bash
bash scripts/demo_v07.sh
```

- [ ] **Step 4: Run green README test**

Run the same README test.

Expected: pass.

## Task 3: Verification

**Files:**
- Verify all changed files.

- [ ] **Step 1: Focused tests**

Run:

```bash
PYTHONPATH=src python3 -m unittest tests.test_verify_script tests.test_readme tests.test_run_plan_cli tests.test_verifier_policy_init_cli -v
```

Expected: pass.

- [ ] **Step 2: Full verification**

Run:

```bash
bash scripts/verify.sh
```

Expected: compileall passes, unittest passes, doctor returns `status: ok`.

## Self-Review

- Spec coverage: temp workspace, optional workspace override, command coverage, README, and full verification are covered.
- Placeholder scan: no deferred implementation notes remain.
- Type consistency: script path and README command match tests.
